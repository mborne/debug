import Metadata from './Metadata';
import Headers from './Headers';

export default function Main(){
    return (
        <div>
            <h1>debug is up!</h1>

            <Metadata />
            <Headers />
        </div>
    );
}
